# Nome Android 下一线程开发交接

> 日期：2026-07-17（Asia/Shanghai）  
> 工作区：`/Users/forkman03/project/nome/simplex-chat`  
> 分支：`codex/nome-android-v656`  
> 官方基线：SimpleX Chat `v6.5.6` / `59fce95d3cd08897b4ef742447b785cf2e56c7ce`  
> 当前阶段：Phase 0 能力审计与 Phase 1 官方基线完成；Nome 生产 UI 源码尚未开始  
> 当前门禁：GAP-08～18、API 26 方案、五张代表暗色页和 Android-only 源码边界尚待明确  
> 远端边界：只读现有官方 `origin`；不得 push

## 1. 本文档的用途与权威顺序

本文档是下一线程的续跑入口，不替代原始总体计划或能力矩阵。新线程在任何
编辑、构建、模拟器启动或设计扩展前，必须完整阅读以下文件，不能只读摘要：

1. [`apps/multiplatform/CODE.md`](../apps/multiplatform/CODE.md)
2. [`plans/20260716_02.md`](20260716_02.md)
3. [`plans/20260716_01.md`](20260716_01.md)
4. 本交接 `plans/20260717_01.md`
5. [`plans/20260716_03.md`](20260716_03.md)
6. [`plans/20260716_04.md`](20260716_04.md)
7. [`product/views/nome-android.md`](../apps/multiplatform/product/views/nome-android.md)
8. [`spec/client/nome-android-ui.md`](../apps/multiplatform/spec/client/nome-android-ui.md)
9. [`product/gaps.md`](../apps/multiplatform/product/gaps.md)

`plans/20260716_02.md` 必须完整阅读，但其中 detached HEAD、native library 尚未
准备、API 26 环境尚缺等内容是当时的启动快照，不是当前状态。不得重复执行其中的
建分支、下载 `latest`、破坏性 prepare 或“补齐缺失环境”步骤；当前事实以
`20260716_03.md`、`20260716_04.md` 和本文档为准。

若当前 Git、设计文件、证据或运行时与本文档不一致，以实时只读检查为准，先说明
漂移，不得通过 reset、覆盖或删除来“恢复”本文记录。

## 2. 不可越过的边界

- 只做 Android；不开发、构建或修改 iOS。
- 使用已经批准的 Nome Android 明亮视觉基线；不得改回旧 Nome 方案，也不得自行
  发明第二套暗色方向。
- 不修改 Haskell/native core、`Core.kt`、数据库格式、协议、消息状态机或 archive
  格式。
- 不从其他 SimpleX/Nome checkout 整批 cherry-pick 旧实现、custom core、Nix 补丁
  或旧 QA 证据。
- Android 专用外壳、主题和组件默认放在 Android 可控路径；只有 Desktop 明确应该
  共享的行为与样式才进入 `commonMain`。每个 `commonMain` 改动必须运行
  `desktopTest` 并检查 Desktop 未被意外 Nome 化。
- 当前 Android-only 范围默认不修改共享平台抽象。若确实必须改变 `expect`
  声明，`CODE.md` 要求同步 Android 与 Desktop 两个 `actual`；新线程必须先说明
  原因并取得范围扩展，不能只改 Android actual 或悄悄进入 Desktop 开发。
- 首版保留现有 Material 2 API，以 token/组件实现目标体验；不顺手做全库
  Material 3 迁移。
- Preview/test fixture 只能位于 Preview/测试路径，不能流入生产状态。
- 不得用 timer、mock、静态 badge 或本地布尔值制造 core 未证明的进度、送达、
  relay 健康、E2EE、过期、回滚或远端使用事实。
- 不修改现有 remote，不 pull，不 push；本交接批次也不提交。后续本地 commit
  仍不等于获得 push 权限。
- 保留用户原有未跟踪文件 `plans/20260716_01.md`、`plans/20260716_02.md`，不要
  清理或覆盖。

## 3. Git 与冻结检查点

交接生成前的实时状态：

| 项目 | 值 |
|---|---|
| branch | `codex/nome-android-v656` |
| HEAD | `59fce95d3cd08897b4ef742447b785cf2e56c7ce` |
| exact tag | `v6.5.6` |
| merge base with tag | 同一 commit |
| remote | 官方 `origin`，URL 未修改 |
| production source changes | `0` |
| prior reviewed snapshot | 63 files / `c4a705f7e121b5786ac6c152d2f78b241fd7eb9eda58017ab697f819122f1ac0` |
| commit/push in this line | 无 |

前一批最终树在无文件编辑的情况下依次得到：

| Round | Result | Reviewer | Frozen digest |
|---|---|---|---|
| 1 | `ROUND 1: ZERO ISSUES` | `/root/review_round1` | `c4a705f7e121b5786ac6c152d2f78b241fd7eb9eda58017ab697f819122f1ac0` |
| 2 | `ROUND 2: ZERO ISSUES` | `/root/pre_record_round2` | `c4a705f7e121b5786ac6c152d2f78b241fd7eb9eda58017ab697f819122f1ac0` |

该 63-file digest 是新增本交接前的冻结点。新增本文档后文件数和 digest 必然变化；
这不代表前一批证据失效。下一线程应先执行：

```bash
cd /Users/forkman03/project/nome/simplex-chat
git status --short
git branch --show-current
git rev-parse HEAD
git describe --tags --exact-match HEAD
git merge-base HEAD v6.5.6
git remote -v
git diff --check
```

不要在未理解现有 dirty tree 前 stash、reset、clean 或 checkout 覆盖。

## 4. 已经完成且不应重做的工作

### 4.1 基线与设计冻结

- 已完整阅读官方交接、产品、spec 和设计权威入口。
- 24/24 张批准 Android PNG 已冻结；每张为 470 × 936，哈希记录在
  [`plans/20260716_03.md`](20260716_03.md)。
- 三份设计权威文档哈希已冻结；不要改用旧 `pages/` 目录或 Design Reset 之前的
  记录作为实现事实。
- 官方 v6.5.6 arm64/armv7 APK、签名、ABI、ELF、SONAME、依赖和 native library
  哈希均已核对。

### 4.2 能力与状态矩阵

- [`plans/20260716_03.md`](20260716_03.md) 已包含 P01–P24 共 264 个执行级状态行；
  每行 12 列，覆盖 zh-CN/en、light/dark、精确字段、API/core/派生规则、A/B/C、
  GAP、product/spec、测试、原生截图、无障碍和真实证据。
- P01–P24 的 summary/detail Class 与 GAP 并集已一致；GAP-08～17 的页面范围已与
  `product/gaps.md` 交叉验证。
- P1 和全部 Android 可达二级功能面已盘点，24 张主效果图不是完整发布边界。
- 源码真值修订已经包含 P14 reject null 歧义、P15 address null 歧义、P17
  `MediaPreview`/`CR.ApiChat`、P18 删除枚举和 report eligibility、P20 删除结果、
  P21 `E2EEInfo.public`、P22 active-user 删除、P23 optimistic network default、
  P24 migration/archive 多个安全边界。不要恢复成较早的乐观表述。

### 4.3 构建与真实 core

- Android Studio JBR 21.0.10、SDK/build tools 35、NDK 23.1.7779620、CMake
  3.22.1 已验证。
- `desktopTest` 强制重跑通过：13 tasks，12 executed。
- Android unit/lint/assemble 强制重跑通过：102 tasks，101 executed。
- 双 ABI CMake Gradle task 运行，但 Ninja 复用了 native objects；不得声称完成了
  fresh native recompile。
- arm64 debug APK 已安装到 API 35 专用 AVD；真实 core/server/receiver、官方
  onboarding、合成身份 `NomeBaselineQA` 和非空本地数据库均已验证。
- 重装新 APK 后未清数据冷启动通过，`TotalTime: 1505 ms`，直接恢复到中文深色
  空会话页。
- 当前生产源码仍是官方 UI；这些截图是 Nome 实现前的 before 证据，不是 P03/P08
  已实现声明。

### 4.4 双语、明暗、无障碍与截图

- 已保留 English/中文 × light/dark 四象限原生截图和 UIAutomator tree。
- 四份 home tree 均为 6 clickable、6/6 可从自身或 descendant 获得 accessible
  name、4 focusable。
- 最终中文深色 tree 使用 `返回`、`资料图片占位符`、`开始新聊天`；旧混合语言
  抓取已替换，不能重新计入。
- `FLAG_SECURE` 黑图先作为官方默认行为保存；之后仅改变隔离包运行时 preference
  以允许截图，未改源码或 APK。
- [`native-screenshot-comparison.md`](evidence/20260716_nome_android_phase1/native-screenshot-comparison.md)
  已保存 P03/P08 approved reference 与原生 before 对照。
- 这批尚未完成 Nome UI 的 200% font、TalkBack traversal、对比度和 48dp 发布门禁；
  只有 baseline semantic tree 通过。

### 4.5 证据闭合

- [`evidence/20260716_nome_android_phase1/README.md`](evidence/20260716_nome_android_phase1/README.md)
  是证据索引。
- `SHA256SUMS` 覆盖 manifest 之外全部 48 个证据文件，已 48/48 通过。
- API 35 可恢复 AVD 位于忽略目录
  `apps/multiplatform/.nome-local/api35-avd/`；完整目录约 7.8 GiB，snapshot 约
  2.5 GiB，`ram.bin` 为 2,636,092,098 bytes。
- 官方 APK 与 `.asc` 位于忽略目录
  `apps/multiplatform/.nome-local/artifacts/v6.5.6/`。
- 上述 `.nome-local` 资产只存在于当前机器的 Git-ignored 路径，不会随 commit、
  新 clone 或远端自动迁移；下一线程只能在实时存在性检查通过后使用。
- API 26 一次性 AVD home 位于 `/tmp/nome-android-v656-baseline-20260716/api26-home/`，
  不是耐久交接资产；耐久事实以 evidence 中的 crash Logcat、ELF symbol 与 NDK
  header 记录为准。
- detached PGP signature 未验证，因为环境没有 `gpg`；只能声称 SHA-256 与 APK
  v2/v3 签名验证通过。
- 不要修改、重抓或重新生成旧 evidence，除非发现具体错误；新增开发批次应创建
  新证据目录。

## 5. 已确认阻塞与当前未决决策

用户尚未在当前线程明确批准以下选择。新线程不得把推荐方案当成已批准事实。

| GAP | 推荐的首版决策 | 未批准前的边界 |
|---|---|---|
| GAP-08 | 只用真实分支和 indeterminate progress；删除虚假百分比/恢复承诺 | P01/P08 相关状态 blocked |
| GAP-09 | 用户启用锁后 fail closed；使用既有恢复路径并加客户端 backoff | P02 unavailable/lockout blocked |
| GAP-10 | 在现有 API 外建立 Android/client operation state、单提交、typed result 映射和 Settings 权限恢复 | 涉及异步/权限/危险操作的页面 blocked |
| GAP-11 | 显式分 scope 的 search coordinator 与本地 settings index；先不保存 recent query | P09/P23 聚合搜索 blocked |
| GAP-12 | 删除倒计时；区分 generated、copied/shared、waiting、真实事件证明的 consumed/connected | P11 expiry/regenerate claims blocked |
| GAP-13 | 删除不支持的 request message；withdrawn 只按消失/事件；address replace 拆成 delete/create 两阶段 | P14/P15 乐观成功 blocked |
| GAP-14 | 不显示未证明的 verified admin；report 只在真实 group route；保留兼容链接，不发明 Nome slug/domain | P16/P18/P20 对应 claims blocked |
| GAP-15 | 持久项只叫本地身份；incognito 只描述为本次连接的独立身份 | P22 持久匿名身份 blocked |
| GAP-16 | 无真实事实时使用 indeterminate；不可逆后禁 cancel；只显示可恢复 checkpoint；不承诺 rollback | P03/P24 扩大实现 blocked |
| GAP-17 | 分开设备连接、配置校验、operator/server、group relay 和 item-level security event | 高层健康/私密路由/E2EE badge blocked |
| GAP-18 | 若 API 26 是硬需求，重建 exact-commit、源码不变的兼容 native artifacts；否则如实提升 minSdk 28 | 发布最低版本 blocked |

### API 26 的已证实结果

在 dedicated API 26 arm64 AVD 上，本地未改源码 debug APK 与官方签名 v6.5.6
arm64 APK 均因 `libsimplex.so` 引用 API 28 才提供的 `getentropy` 而启动崩溃。
官方 armv7 native library 也存在该未解析符号，不能把换 ABI 当作已证明修复。

这属于官方 native artifact/声明最低版本不兼容，不是 Nome UI bug，也不是 Kotlin
wrapper build failure。当前范围禁止 native 源码补丁；exact-commit artifact rebuild
只能在用户明确选择保留 API 26 后进行。

### 暗色与 source placement

- 24 张已批准效果图是明亮视觉基线，不能自动推出完整暗色系统。
- P02、P07、P17、P21、P23 的代表暗色规则/稿件尚未批准。
- 在用户批准 GAP 决策后，先做不进入生产状态路径的设计/Preview 代表稿并取得确认，
  再冻结 dark token；确认前不批量实现暗色页面。
- Phase 2 源码落点尚未最终冻结：Android host/system integration 留在
  `android/src/main` 与 `common/src/androidMain`；Android-only Nome shell/theme/
  components 默认采用 Android-controlled 路径；共享组件只有在 Desktop 应明确
  共享时才可进入 `commonMain`。

## 6. 下一线程的合法执行顺序

### Step 0 — 只读恢复现场

1. 按第 1 节顺序完整阅读文件。
2. 运行第 3 节 Git 检查，确认 HEAD/tag/branch/remote 和 dirty tree。
3. 在正确目录运行旧 evidence 校验：
   `(cd plans/evidence/20260716_nome_android_phase1 && sha256sum -c SHA256SUMS)`；
   不要改文件。
4. 确认不存在其他线程在编辑同一批文件；发现漂移先报告。

### Step 1 — 取得并落盘产品决策

必须由用户明确选择：

1. GAP-08～17 是否全部采用上表推荐方案，或逐项给出不同选择；
2. GAP-18 采用 `minSdk 28`，还是必须保留 API 26 并做 exact-commit artifact rebuild；
3. 是否允许先产出 P02/P07/P17/P21/P23 五张代表暗色稿供批准。

收到决策后，同步更新 `product/gaps.md`、`product/views/nome-android.md`、
`spec/client/nome-android-ui.md`、能力矩阵和新的执行记录。不要只把决定留在聊天里。

### Step 2 — 暗色代表与 Android-only 边界冻结

1. 基于已批准明亮视觉、现有 Nome tokens 和五个指定页面产出代表暗色规则/稿件；
   不自行改变信息架构。
2. 获取用户明确批准，并记录批准输入与资产哈希。
3. 写出最小 Phase 2 文件/组件预算和 Android-only source placement；没有明确共享理由
   不进入 `commonMain`。
4. 新建下一批执行记录和独立 evidence 目录，不覆盖 Phase 0/1 证据。

### Step 3 — Phase 2 设计基础小批次

只做 tokens、theme adapter、基础组件、Preview/test fixture、截图 harness 和必要的
Android shell；不要同时开始 P01–P10 大批页面。该小批次必须同时完成：

- zh-CN 与 English；
- light 与已批准 dark；
- 适用的 normal/loading/empty/offline/error/permission/danger states；
- 200% font、48dp touch target、TalkBack semantics/order、非纯颜色表达；
- 自动测试；
- 真实 core/model/API trace；
- 原生截图与批准稿并排/叠加审查；
- 若触碰 `commonMain`，运行 Desktop 测试和视觉/行为回归；
- threat-model delta；
- 完成后连续两轮 `ZERO ISSUES`，并把结果写入执行记录。

### Step 4 — 再进入页面批次

Phase 2 小批次通过后，才按总体计划从 P01–P10 分小批实现。每批都必须同步
product/spec/source/test/evidence；不能只完成 happy path，也不能把 24 张效果图当作
全部 Android 功能面。

## 7. 不得误报的事项

- 不得说 Nome Android UI 已实现；当前生产 UI 仍是官方基线。
- 不得说 API 26 支持；已确认 FAIL / GAP-18。
- 不得说 native objects 已重新编译；Gradle CMake task 运行但 Ninja 复用对象。
- 不得说 PGP 已验证；环境没有 `gpg`。
- 不得把黑色 FLAG_SECURE 截图当 UI failure。
- 不得把 API 35 device connectivity 当 private routing、relay health 或 E2EE 证明。
- 不得把 copied/shared invitation 当远端已使用。
- 不得仅凭 migration `Finished`、未经验证的本地 checkpoint 或 optimistic default
  就宣称成功或通用可恢复；只能按矩阵中已证明的 stage-specific recovery 表述。
- 不得重跑、覆盖或“美化”旧证据来制造新的 PASS。
- 不得把本地 commit 权限解释为 remote push 权限。

## 8. 建议用户决策短句

若用户接受推荐路线，可直接回复：

```text
GAP-08～17 全部按推荐方案；GAP-18 提升 minSdk 到 28；先产出 P02/P07/P17/P21/P23 五张代表暗色稿给我确认，然后冻结 Android-only 组件边界并开始 Phase 2 小批次。
```

若 API 26 是商业硬要求，将其中 `提升 minSdk 到 28` 替换为：

```text
必须保留 API 26；只允许 exact v6.5.6 commit、native 源码不变的兼容 artifact rebuild，并重新做双 ABI provenance、API 26/API 35、非空数据库升级和完整发布门禁。
```

## 9. 新线程可直接粘贴的启动提示词

```text
在 /Users/forkman03/project/nome/simplex-chat 继续 Nome Android 原生开发。先完整阅读 apps/multiplatform/CODE.md、plans/20260716_02.md、plans/20260716_01.md、plans/20260717_01.md、plans/20260716_03.md、plans/20260716_04.md，并实时核对 git 状态；严格保留官方 v6.5.6 / 59fce95d 基线和当前 dirty tree，不 reset/clean/stash 覆盖，不修改 Haskell/native core、Core.kt、数据库/协议/消息状态机，不开发 iOS，不推远端。Phase 0/1、264 状态矩阵、API 35 真实 core、48/48 证据和既有双语明暗 baseline 已完成，不要重做；API 26 为 GAP-18。先按交接取得并落盘 GAP-08～18、API 26/minSdk 和 P02/P07/P17/P21/P23 暗色代表决策，再冻结 Android-only source placement，之后只做 Phase 2 设计基础小批次；每批必须双语、明暗、全部适用状态、200%/48dp/TalkBack、真实 core、原生截图对照、threat-model delta 和连续两轮 ZERO ISSUES。
```

## 10. 本交接批次 threat-model delta

| 项目 | 结论 |
|---|---|
| 新增 production entry point | 无；仅新增本地 Markdown 交接文件 |
| 新增 attacker-controlled input | 无；未启动 app、intent、网络、模拟器或 core 命令 |
| 受保护资产 | 官方基线/证据完整性、用户未跟踪计划、未决决策来源、禁止 push 边界 |
| 控制 | 实时 Git/哈希检查、只读引用旧证据、不保存 secret、不修改 remote/production source |
| 残余风险 | 交接可能随未来工作树变化而过期；下一线程必须先做 Step 0 实时复核，且不得把推荐选择当用户批准 |

## 11. 本交接文档审查记录

本节在两位只读审查者检查同一份预记录文档后写入结果；写入后再对最终字节执行
连续两轮只读复核。结果未写入前，本文档不能宣称交接完成。

`Snapshot digest` 是结果写入前本文件
`plans/20260717_01.md` 原始字节的 SHA-256；不做换行、Unicode 或 Markdown
规范化，可在仓库根目录用 `shasum -a 256 plans/20260717_01.md` 独立复算。仓库其余
前置状态由第 3 节的 63-file frozen digest 绑定。

| Pre-record round | Result | Reviewer | Snapshot digest |
|---|---|---|---|
| 1 | `HANDOFF PRE-RECORD ROUND 1: ZERO ISSUES` | `/root/source_matrix_audit` | `43ffe64c11a9a06a2b0570dcd971d783a25e924fc4bbc6b6b86d1746ab5df8fb` |
| 2 | `HANDOFF PRE-RECORD ROUND 2: ZERO ISSUES` | `/root/handoff_review2` | `43ffe64c11a9a06a2b0570dcd971d783a25e924fc4bbc6b6b86d1746ab5df8fb` |
