# Nome Android Final Local Release-Candidate Verification

Date: 2026-07-20
Branch: `codex/nome-android-v656`
Frozen input: `3fa40fc8677ea894a80693d576caffd178275b80`
Distribution boundary: `NOT FOR DISTRIBUTION`
Status: determined by the committed `FINAL_REVIEW_VERDICTS.md`; `NOT FOR DISTRIBUTION`

This report is the concentrated final verification record permitted by the user's amended
execution contract. It reuses the frozen Foundation, P01–P10, P13, and Milestone 3 evidence and
does not repeat ordinary page batches. A result is closed only by the real official
API/core/platform/peer fact required for that result.

The user's 2026-07-20 completion amendment changes only the terminal treatment of the retained
public-channel Observer result. Owner lifecycle is verified; Observer membership and post receipt
remain explicitly not verified after the actual-link relays failed observer-side official tests.
That declared external validation gap no longer blocks the local RC, but it cannot be represented
as membership, receipt, online state, relay health, delivery, or success.

P01–P24 are page-level visual acceptance baselines. Final reconciliation must keep each accepted
Android production page aligned in composition, placement, hierarchy, color, typography, spacing,
corners, iconography, action size, and density; shared theme/components alone are not acceptance.
The official v6.5.6 route, state, operation, data, and interaction owners remain authoritative.
The 2026-07-19 P02–P06 re-inspection retained the accepted same-size comparisons and replaced the
last adjacent text-only onboarding wordmark seam with the approved compact image asset.

## Section 6 acceptance audit

| # | Current result | Reusable evidence or exact remaining gate |
|---:|---|---|
| 1 | `COMPLETE` | P01–P24 routes/classification are mapped and terminally classified. P11 and P14 are producer-backed and visually accepted. P16's real private invitation is producer-backed and structurally calibrated, while the reference's materially different public-invitation visual state is terminal `NOT REACHABLE LOCALLY`. P21's real owner state is producer-backed and compared, while the reference's observer-only visual state is likewise terminal `NOT REACHABLE LOCALLY` rather than falsely accepted. |
| 2 | `COMPLETE` | All 14 Android-reachable P1 families are mapped and locally adapted; the last remote family now has a controlled official API 28 host/API 35 Android-controller pair/verify/switch/stop/delete/list-absence lifecycle in addition to its accepted unpaired presentation. |
| 3 | `COMPLETE` | Product/spec/source/tests/ledger prohibit inferred success, online, delivery, security, restore, percentage, timeout, or peer-use facts. |
| 4 | `COMPLETE` | `FIRST_USE` remains official onboarding/root-owned; frozen P08 evidence was not rewritten. |
| 5 | `COMPLETE` | `FILTERED_NO_RESULT` is produced only by P09's real nonblank official query. |
| 6 | `COMPLETE` | Milestone 3 passed zh-CN/English, light/dark, and 100%/200% on the populated API 35 production client. |
| 7 | `COMPLETE` | Ordinary family 48dp/semantics checks plus real Milestone 3 TalkBack traversal/activation/restoration pass. |
| 8 | `COMPLETE` | API 28/API 33/API 35 device suites, clean/fresh depth, authentic non-empty same-package upgrade, and full Milestone 3 regression pass. |
| 9 | `COMPLETE — DECLARED EXTERNAL GAP` | Real controlled two-client ready connection, direct-message peer receipt, private-group invitation/join, group-message peer receipt, synthetic image-file receipt/download/external-open/cleanup, incoming/answer/connected-call/clean-end, fixed synthetic voice/video receipt/digest/playback/stop/cleanup, and public-channel owner create/link/populate/open/delete/absence pass. Under the user's 2026-07-20 completion amendment, public-channel Observer membership/receipt remains explicitly not verified and is carried as a non-blocking external validation gap. |
| 10 | `COMPLETE` | Disposable API 28/API 35 export/import integrity, key re-entry, rollback marker, migration abort/restart, and cleanup pass; unsupported success/resume claims are absent. |
| 11 | `COMPLETE` | Desktop suite/fallbacks pass and Desktop retains the official composition. |
| 12 | `COMPLETE` | Frozen-path and staged-path audits show no Haskell/native core, `Core.kt`, protocol, database semantic, archive-format, or iOS change. |
| 13 | `COMPLETE` | Release ZIP/DEX/manifest scans found no test fixture, observability hook, or evidence path in the unsigned release APKs. |
| 14 | `COMPLETE` | Retained evidence and release scans found no real bearer, token, private key, QR, signature, identity, or server credential. The P14 production-capture harness fails closed on API 35 without the fixed opt-in token unless the explicit general-regression producer bypass is present; an actual capture remains restricted to the fixed controlled identities and exact token, redacts both identity regions in memory before the first file write, and emits only the redacted filename. |
| 15 | `COMPLETE — CURRENT SOURCE` | The final serialized Android unit/lint/debug/androidTest/release/bundle aggregate, separate Desktop suite, and exact current main/test APK API 28/API 33/API 35 88-test general-regression matrix all pass. Its explicit controlled-producer bypass means it is not producer-execution evidence; all 13 argument-driven producer/lifecycle harnesses and the separate P14 guarded capture fail closed without their required inputs, and real producer/lifecycle/capture claims remain tied to focused controlled runs. Release lint has 0 fatal / 0 errors; release-isolation, artifact, document, manifest, and restoration checks are current. |
| 16 | `COMPLETE — CURRENT SOURCE` | Product/spec/source/tests/ledger are synchronized through the current change; 44 documents / 1,209 local links / 884 heading and line anchors have zero findings. |
| 17 | `FINALIZED BY COMMITTED VERDICT RECORD` | This immutable review-input report does not embed its own mutable final verdicts. Criterion 17 is complete only when the committed `FINAL_REVIEW_VERDICTS.md` records independent code/product/spec/test/evidence Round 1 and security/release/privacy/evidence Round 2 `ZERO ISSUES` against the exact same self-verifying 71-entry `REVIEW_INPUT_SHA256SUMS`, with no manifest-listed byte changed between or after rounds. |
| 18 | `FINALIZED BY COMMITTED MANIFEST` | Current-source unsigned APK/AAB hashes, badging, native ABIs, unsigned truth, isolation, and device results are audited and reproducible. Criterion 18 is complete only when the committed final `SHA256SUMS` verifies every frozen payload file, the self-verifying review manifest, and `FINAL_REVIEW_VERDICTS.md`. |
| 19 | `FINALIZED BY CONTAINING LOCAL CHECKPOINT` | Foundation/P13/Milestones 1–3 checkpoints remain local with no push. Criterion 19 is complete when the local commit containing this report also contains the verified final manifest and completed verdict record; its exact hash is reported by Git after creation. |
| 20 | `COMPLETE — DECLARED NON-BLOCKING GAP` | Ledger, matrix, and this report agree that public-channel Observer membership/post receipt is not verified. On the third consecutive turn the exact current test APK found both actual short-link relays failing observer-side official tests at `Connect`, before prepare/connect, while source membership and target model/core state stayed unchanged and clean. The user's 2026-07-20 completion amendment authorizes local-RC closure with this result declared consistently and without a success claim. |
| 21 | `COMPLETE — NOT FOR DISTRIBUTION` | AGPL/asset/brand/application identity/App Links/privacy/store-material local audit is complete; missing external publication inputs are explicitly declared. |
| 22 | `COMPLETE` | Populated API 35 production Home/Settings/Privacy/direct-chat passed English 1.3 expansion with animation scales zero at 360×640dp small portrait, 411×914dp long portrait, and 640×360dp small landscape; P0 routes had no blocking break at 800×1280dp tablet and 674×841dp foldable-inner bounds. The discovered P17 expanded E2EE-banner clipping was fixed and focused-tested; exact display/language/motion/privacy settings were restored. |

## Active producer lane

The earlier controlled attempts showed official `smp9.simplex.im` and `smp12.simplex.im`
connection errors; those failures remain historical and are not rewritten. Their owner oracle was
later proven incomplete: official `ReceivedContactRequest` handling normally materializes a
pending `ChatInfo.Direct` with `contactRequestId`, while only the null-chat form is
`ChatInfo.ContactRequest`. The retained API 35 production client already contained that real
pending Direct shape. Android Home now exposes the official request route for both shapes, the
same official accept/reject and post-accept owners remain intact, and the production P14 page
passed its same-size Chinese/light comparison with screenshot protection restored. P14 is closed;
none of the earlier network failures is relabelled as request delivery. The recovered official
connection path independently proves a new ready direct contact on both clients, peer receipt of a
direct message, and a private-group invitation/join/message round trip. P16 also has a real API 35
Chinese/light private-invitation production capture and structural comparison. Because the
reference is a public invitation with different membership/inviter facts, its visual state is
terminally `NOT REACHABLE LOCALLY` rather than accepted from that comparison. The
archive, database key, and connection bearer stayed ephemeral and outside evidence; the disposable
clone was cleared and screenshot protection was restored.

The controlled official file-transfer path is closed: exact peer receipt, download bytes,
installed external image viewer launch, return, and controlled fixture cleanup pass on API 35 to
API 28. The connected-call path is also closed on two API 35 production clients: the source placed
the official audio call, the target accepted the new official invitation, both reached
`CallState.Connected` with connection metadata, and official/remote teardown completed cleanly.
The original API 35-to-API 28 attempt remains a recorded WebView 66 platform failure rather than
success. The public-channel owner path is now closed by a real single-process API 35
create/link/populate/open/delete/absence lifecycle and P21 Chinese/light same-size owner
comparison. That image is not acceptance of the reference's observer-only state. The displayed
local attachment is not remote-file evidence, and failed observer attempts are not promoted.
Fixed synthetic AAC and H.264/AAC fixtures independently pass official
API 35 send, API 28 peer receipt, exact decrypted SHA-256, stable repeated reads, production
audio/video progress/termination, stop, and cleanup; temporary XFTP selection was restored to each
client's prior SimpleX hosts with Flux disabled. Earlier decryption/connection failures remain
historical. Remote pairing is independently closed by a controlled API 28 remote-host/API 35
Android-controller run through the official invitation/connect/session-code verify/connected/
switch-local/stop/delete owners. Both official lists exclude the controlled pair after deletion;
the invitation stayed in ephemeral host memory, temporary loopback routes were removed, and no
scanner state was promoted. No bearer, account credential, protocol/core change, custom server
deployment, or inferred result is permitted.

The historical public-channel observer and P14 producer alternatives were retried with
passing-server diagnostics and no retained bearer. Two fresh owner channels using different configured-relay
offsets produced API 28 `Pending` without a member/channel on either peer for 300 seconds, then
sanitized API 33 and disposable API 35 `failure/error agent` results; both controlled channels
were officially deleted. For P14, the API 33 requester was temporarily narrowed to its passing
SimpleX SMP preset and reached `Pending`, but the retained API 35 owner received no request in
300 seconds. A disposable API 35 owner with its own newly created official address and passing
SimpleX SMP preset produced the same requester `Pending`/owner-absence result. A final independent
combination then used API 28's officially tested passing `smp10.simplex.im` requester against that
disposable owner: the requester again reached real `Pending`, while the owner received no
`ChatInfo.ContactRequest` in 300 seconds. Exact prior API 33/disposable-owner SimpleX SMP
selections were restored with official validation and zero errors; API 28 already had only its
original `smp10` selection. These runs do not close two-client public-channel receipt and are not
rewritten as connectivity, server health, membership, or delivery success; their P14 absence
interpretation is superseded by the corrected official Direct-request oracle above.

The untested public-channel direction was then boundedly exercised from the API 28 client already
using its passing `smp10.simplex.im` preset. An official owner-create attempt with another
configured-relay offset produced no terminal result for 513.78 seconds. The controlled test/app
processes were stopped without clearing data, and a following official status test passed in
2.647 seconds with an empty controlled public-channel list. The instrumentation's resulting
`Process crashed` short message records the deliberate process stop, not a product exception.
No channel, link, observer, post receipt, timeout fact, or success is inferred from this attempt.

The final stale-queue alternatives were also exercised without changing product/core behavior.
API 28 and disposable API 35 both selected `smp8.simplex.im` only after an official protocol PASS
and zero-error server validation. A disposable-only androidTest guard then deleted/recreated the
owner address twice. Both fresh 307-byte links stayed inside the redacted memory bridge; the
requester reached real `Pending` twice, while the owner received no new `ChatInfo.ContactRequest`
within either 300-second window. The first window included a system Wi-Fi reconnect, the second
did not. For public channels, a fresh disposable API 35 observer reached `Pending` from the
persisted owner link without a member/channel in 300 seconds. A second disposable-only guard
deleted the controlled owner's old group link and created a fresh official Observer link; that
retry again reached `Pending` without a member/channel in 300 seconds. A separate disposable API
35 owner-create call returned official `Created` in 15.209 seconds, but the following process
restart/status reported no persisted controlled channel. Filtered Nome FATAL/ANR counts were zero.
None of these negative results is promoted to request delivery, channel persistence, observer
membership, post receipt, server health, or online state.

After the P14 production route and the concentrated device matrix were closed, the remaining
public-channel gate received one further same-version API 35-to-API 35 attempt. The official
Observer link crossed only a bounded host-memory bridge and the requester returned the sanitized
real `Pending` result. The source then failed after 308.182 seconds because its official member
list contained no active Observer; the target failed after 310.524 seconds because it contained no
ready Observer channel or either controlled post. Filtered Nome FATAL/ANR counts were zero on both
clients. This attempt is retained as negative external producer evidence and is not promoted to
membership, persistence, receipt, server health, or online state.

A subsequent read-only audit of the official v6.5.6 command path found that those earlier
public-channel attempts used the wrong connection owner: channel short links must be prepared with
`apiPrepareGroup` and started with `apiConnectPreparedGroup`; ordinary `apiConnectResult` is not the
production channel-join path. The androidTest producer was corrected without changing release
source, core, protocol, or database semantics. Five stale controlled `Joined` contact connections
were officially removed from the disposable observer. A 70-byte short link then crossed only the
bounded memory bridge and produced a real prepared `Observer / MemUnknown` channel with
`preparedConnection=true`, `startedConnection=false`, plus two `Relay / MemAccepted` members. The
owner still had no Observer after 307.611 seconds and the target connect-prepared call remained
non-terminal beyond that window. One bounded resume returned the local controlled-data constraint
`UNIQUE constraint failed: group_members.group_id, group_members.member_id`; the stale prepared
channel was officially deleted rather than modifying storage or core. A completely fresh corrected
run then reproduced owner absence after 307.590 seconds while target connect-prepared again had no
terminal result. The disposable prepared channel was officially deleted and a post-cleanup
diagnostic reported zero prepared public channels. A final new one-relay owner-create attempt
remained non-terminal for more than 300 seconds; post-stop status found no new persisted channel
and the retained owner still showed only two connected relays, one invited relay, and zero
Observers. None of these facts is promoted to timeout, relay health, membership, receipt, online,
or success. This is the first Goal-turn occurrence of the now-corrected external blocker.

## Responsive and reduced-motion closure

The production client was switched through the official Appearance route to English, then tested
with system font scale 1.3 and all three animation scales set to zero. The 30% expansion/reduced-
motion stress retained Home, Settings, Privacy and security, direct-conversation facts, and their
primary actions at the small/long/landscape phone bounds and the tablet/foldable-inner bounds
listed in criterion 22. UI hierarchies retained the relevant Back, More, Search, new connection,
attach, record, send, bottom-navigation, and settings actions.

The first small-screen direct-chat run exposed a genuine fixed-height E2EE-disclosure clipping
defect. The Android presentation now reserves a font-scale-aware banner height in both the chrome
and chat-list inset; a focused common test covers 85%, 100%, 130%, and 200%. The focused Desktop
test and Android debug assembly passed. Production re-inspection retained the complete disclosure
and reachable conversation actions with no Nome FATAL/ANR. Cleanup restored physical
1080×2400/420dpi, portrait with auto-rotate, font scale 1.0, default animation scales, official
Simplified Chinese, and screenshot protection (`colors=1`, mean `0.25`). Temporary responsive
screenshots stayed outside the repository.

## Final host, documentation, and unsigned-artifact aggregate

Earlier, before the final current-source rerun, a post-P21-file-interaction-correction serialized
Gradle aggregate ran
`:android:testDebugUnitTest :android:lintRelease :android:assembleDebug
:android:assembleDebugAndroidTest :android:assembleRelease :android:bundleRelease` and completed
`BUILD SUCCESSFUL in 4m 36s` across 205 tasks (66 executed / 139 up-to-date). Android JVM tests
report 42 tests with
zero failures, errors, or skips; release lint reports 0 errors and 68 warnings. The separate
`:common:desktopTest` run completed `BUILD SUCCESSFUL in 9s` across 13 tasks
(4 executed / 9 up-to-date) with 42 tests and zero failures, errors, or skips. Task discovery
confirmed that `bundleRelease` is an applicable task.

The resulting unsigned universal APK is
`apps/multiplatform/android/build/outputs/apk/release/android-release-unsigned.apk`: 498,412,556
bytes, 1,730 ZIP entries, SHA-256
`f08bdbceacadd09b1162de82019fb91fb93db8f1d103d34825d6a47de9414695`. `aapt` reports package
`chat.simplex.app`, version code 358 / version name 6.5.6, min API 28, target API 35, label `Nome`,
and native ABIs `arm64-v8a` and `armeabi-v7a`. `apksigner verify` correctly returns
`DOES NOT VERIFY` with no signing manifest: this is an unsigned local audit artifact, not a
distributable build.

The release bundle is
`apps/multiplatform/android/build/outputs/bundle/release/android-release.aab`: 117,299,693 bytes,
1,735 ZIP entries, SHA-256
`9318126a0742101a65563903319589bbac8b633f7978cf18d275c9fd0309c33b`. It has no `META-INF`
signature entries, and `jarsigner -verify` reports no manifest / unsigned JAR. APK/AAB entry-name
and DEX-string scans found zero occurrences of the controlled androidTest classes/tags, evidence
paths, the unknown workspace screenshot, or the final verification report.

The pre-final-summary Markdown cross-reference check covered 43 product/spec/completion documents, 1,209
local links, and 884 heading/line anchors with zero findings. Historical evidence was
rehashed without modifying or checking out its frozen inputs: Phase 1 48/48, Foundation 131/131,
Batch 2 254/254, and P13 139/139. P13 was evaluated from checkpoint
`b714f78efc7b3cc8c4e252d6351c4b629f60e277`, because later authorized source changes are outside
that frozen manifest.

After the public-channel androidTest oracle was first corrected and its temporary mutating resume
diagnostic removed, that test source passed `:android:assembleDebugAndroidTest` again in 12s. That
rebuilt test APK SHA-256 is
`ffe8d83cc05c9785fc1e6f36a2841931ebfcc60d45eb57e026f77d617dd056f7`; its API 35 focused
foundation suite passed all six tests in 14.833s, including the approved 104 × 44 dp onboarding
image slot. At that pre-final-summary stage, a fresh documentation check still reported
43 documents / 1,209 local links / 884
heading-or-line anchors / zero findings. Diff, staged, forbidden-source, historical-evidence, and
secret-bearing-pattern audits are clean, and the two disposable producer clients have zero
filtered Nome FATAL/ANR records after the corrected attempts. This is focused post-diagnostic
evidence; the final full device/host aggregate was still deliberately open at that stage.

A later 2026-07-20 read-only relay preflight/core-reload diagnostic also compiled in 12s. Its
intermediate test APK SHA-256 was
`ccaef3bd9747b5373e3a29e2c9d60712f5100e9d3e43195987ae620f761db9bd`.
It found no hidden model/core Observer state, stopped before prepare/connect when both actual-link
relays failed official tests, reproduced `GetLink / BROKER/NETWORK` on a separate API 35 observer,
and found no persisted result after a different one-relay owner create was stopped and cold
reloaded. This remains negative external evidence only.

The third consecutive Goal-turn audit reused that exact test APK. The owner passed all three
configured official relay tests; the observer's configured tests stopped at
`Connect / Connect / GetLink`, all sanitized `BROKER/NETWORK`. Both relay addresses in a fresh
70-byte owner short link then stopped at `Connect`, producing the safe count
`plannedRelayCount=2`, `plannedRelayTestStateCounts=Connect=2`, and
`plannedRelayTestFailures=2`. The fixture failed closed before prepare/connect. Afterward, the
source still had two connected relays, one invited relay, and zero Observers; target model and
official core reloads agreed on zero prepared, model-only, or core-only channel state. Filtered
Nome FATAL/ANR remained zero on both clients. This is the third occurrence of the same external
blocker. Recovery requires every relay actually carried by a newly generated owner short link to
pass observer-side official testing and the owner create/link result to persist, followed by real
owner Observer membership, a ready target Observer channel, and controlled peer-post receipt; the
only alternative is a plan-authorized terminal upstream/core/relay conclusion.

The same newly built debug main/test APK pair was then installed in place without clearing
controlled data. API 28 and API 33 each passed `OK (88 tests)` in 54.782s and 35.784s; at that
historical stage the six API-35 production/evidence screenshot actions returned without side
effects there. Those historical general-regression counts are not producer-execution evidence.
API 35 ran the complete suite including every screenshot action
and passed `OK (88 tests)` in 272.014s. The first API 35 full-suite attempt was deliberately stopped
after 763 seconds while `NomeDatabaseRootScreenshotTest` waited for Compose idle on the real
infinite database `Opening` animation. This is a screenshot-harness idle condition, not a product
exception or database result. With system animation scales temporarily set to zero, the exact
class passed `OK (1 test)` in 80.621s / 81s before the complete successful 88-test rerun. Filtered Nome
FATAL/ANR counts were zero on all three devices. Exact-component cold starts then passed on all
three. Production restart checks
preserved the controlled API 28/API 33 clients and returned API 35 to populated Chinese Home.
The older API 33 genuine-first-use observation is historical, not current, after the authorized
producer identity work. API 35 was restored to 1080×2400 at 420dpi, font scale 1.0,
portrait/auto-rotate, default animation scales, no accessibility service, no per-app system locale
override, and checked screenshot protection; the secure framebuffer again contained one color
with mean `0.25`.

After the user's 2026-07-20 completion amendment, the final current-source aggregate reran from the
same frozen input plus the intended dirty scope. The Android aggregate completed
`BUILD SUCCESSFUL in 3m 33s` across 205 tasks (31 executed / 174 up-to-date), with 42 JVM tests,
zero failures/errors/skips, release lint 0 fatal / 0 errors / 68 warnings, and current debug,
androidTest, unsigned release APK, and unsigned AAB outputs. It used
`--no-daemon -Pkotlin.compiler.execution.strategy=in-process` after an earlier daemon-backed
compile stopped making progress and was interrupted without a product-failure claim. Desktop
completed `BUILD SUCCESSFUL in 1s` across 13 tasks (3 executed / 10 up-to-date), with 42 tests and
zero failures/errors/skips.

The exact debug main APK SHA-256 is
`c0527a1870a63ae6de00a73888c227a4006f47cfc51f925571e04f55e77ca94e`; the exact androidTest APK
SHA-256 is `45c79a70e787a84116c6258a56731b7c5fa97a77f1574caefa393f946772ee59`.
API 28 passed `OK (88 tests)` in 93.514s and API 33 passed `OK (88 tests)` in 138.579s with
explicit `nomeControlledProducerSkip=true` and `nomeCrossApiScreenshotSkip=true`. API 35 passed
`OK (88 tests)` in 511.513s with only `nomeControlledProducerSkip=true`, so every API-35 capture
body executed normally. The general matrix is regression/packaging evidence, not
producer-execution evidence. On API 28, the exact 13 argument-driven controlled harnesses produced
the expected 13 failures in 0.066s with no bypass and `OK (13 tests)` in 0.066s only with explicit
`nomeControlledProducerSkip=true`. Their real producer/lifecycle claims remain supported by
focused controlled fixture runs. API 28 and API 33 retained their controlled clients. The API 35
package was absent before the earlier fresh install, then its disposable client completed official
Chinese onboarding with a synthetic local identity. The latest in-place install retained that
local fixture and the real populated Home. The first current-source API 35 attempt at animation
scales 1/1/1 was stopped while the database screenshot waited for Compose idle at item 43; it has
no elapsed-time or product-timeout claim and is not a valid matrix result. The successful rerun
used animation scales 0/0/0 and restoration to 1/1/1 was verified afterward.
The resolved `MainActivity_default` explicit cold start completed in 2.387s. API 33 completed in
1.189s; API 28 reported `ThisTime=2.749s` / `WaitTime=2.781s`, and its stale task-level
`TotalTime` was not treated as launch duration.
All three exact-package cold starts completed and filtered Nome FATAL/ANR counts are zero. API 35
was restored to 1080×2400/420dpi, font 1.0, portrait/auto-rotate, animation scales 1, no enabled
accessibility service, no system per-app locale override, populated Chinese Home, and checked
screenshot protection (`colors=1`, mean `0.25`). Temporary captures were removed.

The first final-summary review found that the prior P14 production-capture selector could choose
an unrelated newest request and write an unredacted bitmap. That review is invalid and does not
count. An actual capture requires the exact controlled token, current user `IncogTarget35`, and
requester `P21Observer35`; the general matrix may bypass the capture only with explicit
`nomeControlledProducerSkip=true`. The harness keeps the raw bitmap in memory, redacts the
requester card and local-identity row before the first file write, and removes the legacy raw
filename. The exact guarded API 35 test passed `OK (1 test)` in 4.42s. Its directory contained
only `P14-production-request-api35-zh-light-redacted.png`, 127,290 bytes, SHA-256
`352ff9807852d359a42ce30c22663b5d8a518b34b38009c9eb193116348d87bc`; that temporary verification
output was removed and the directory returned to zero files. The current aggregate and 88-test
matrix above use the rebuilt test APK containing this fix.

A later review attempt found that three retained P14 evidence PNGs still exposed the two controlled
identity labels and that the pending `Direct + contactRequestId` Home row announced direct-chat
semantics to TalkBack. That attempt is also invalid and does not count. The old PNGs were moved
outside the workspace without deleting history and replaced by the three `-redacted-v2` files.
The Home description now exposes the contact-request semantic for both official pending shapes.
The focused test passed on API 28/33/35 in 1.102s / 1.773s / 1.869s, followed by the complete
88-test matrix above.

A subsequent review attempt found that the Android public-channel renderer dropped official
non-message timeline items and that the post-text projection trimmed real whitespace. That
attempt is invalid and does not count. The presentation now leaves all non-message membership,
history, security, and other timeline items on the official legacy renderer, limits Nome cards to
simple text/file message content, and preserves the original first-line/body whitespace. The new
regression test is included in the 42-test Desktop suite, and the current aggregate plus all three
88-test device runs above use the corrected source.

A later review attempt found that the P21 owner/composer production image and the
observer/read-only reference were different semantic states, so the owner comparison could not
close the observer visual baseline. That attempt is invalid and does not count. The retained image
is now classified only as owner-presentation/shared-structure evidence. Product, spec, matrix,
ledger, and final summary all mark the reference's observer-only visual state externally
`NOT REACHABLE LOCALLY` under the same declared non-blocking Observer producer gap.

The next review attempt reported `groupDirectInv` as a Home-routing defect. Exact comparison
against official v6.5.6 disproved that finding: the Direct chat opens first and
`ComposeContextMemberContactActionsView` owns group-invitation acceptance in-chat, while the
standalone contact-request page remains limited to a non-null `contactRequestId`. Android comments
now make that unchanged official ownership explicit.

The same attempt found a real P21 deep-link regression. Forward-layout channel mode created a
fixed index-zero `LazyListState` before resolving `openAroundItemId`, so a requested post could be
ignored. The channel route now resolves the official target item first and initializes the
forward list at that index, while the ordinary no-target path remains index zero. A regression
test covers both paths and the real eligible file-message boundary. The current 42-test Desktop
suite, Android aggregate, and three-device 88-test matrix above all include this fix. The review
attempt is invalid and does not count toward either final round.

A following review attempt found that every eligible P21 `MCFile` card replaced the official
interactive `CIFileView` with a static filename/size row. That removed production receive, wait,
progress, error, open, and save behavior, so the attempt is invalid and does not count. File posts
now embed the official `CIFileView`, its real file object, sender profile, menu state, and
`receiveFile` callback inside the Nome card. Text cards remain presentation-only and all
non-message timeline items remain on the legacy renderer. Focused coverage, the 42-test Desktop
suite, Android aggregate, and exact current API 28/API 33/API 35 88-test matrix all include the
correction.

The next review attempt found that `NomeNetworkProducerDiagnosticTest` still wrote concrete
SMP/XFTP hostnames to logcat even though the controlled seam promises no address or credential
logging. That attempt is invalid and does not count. Hostnames now remain only in memory for exact
selection and restoration assertions; logcat contains only operator/protocol counts, preset
booleans, validation totals, and sanitized failure classes. The focused androidTest compile,
source scan, and current rebuilt verification include this correction.

The next same-digest review pair did not close. Round 1 returned `ZERO ISSUES`, but Round 2 found
that the public-channel producer and observer fixtures selected their mutation target through a
broad display-name heuristic and that the status action emitted unrelated channel names. Both
verdicts are invalid. Both fixtures now require the exact test-written user/remote-host/group-id/
nonce record and refuse mutation without it; the status result contains only counts and booleans.
Focused guard/status checks and bounded real producer attempts exercise the correction; the
rebuilt host aggregate packages it. The general 88/88/88 matrix uses the explicit producer bypass
and is not producer-execution evidence.
A bounded live create attempt was stopped after 571.15s before an official result or fixture record
existed; force-stop left the record absent, and the subsequent read-only status action passed
`OK (1 test)` in 2.316s with zero public channels and no names. This is an invalid network-harness
attempt, not a success or product-timeout claim.

The next Round 1 returned `ZERO ISSUES`, but independent Round 2 found that the nonce field in
that exact record was checked only for nonblank text and discarded. A stale or tampered record
could therefore redirect a controlled mutation by changing only its group id. Both verdicts are
invalid. Record-bound producer actions and the two-client source now require a caller-supplied
canonical UUID, persist that exact value, and load a record only when its stored nonce equals the
supplied nonce together with the expected active user, remote host, and group id. API 35 negative
guards prove missing and malformed inputs fail closed; a canonical read-only status check passed
`OK (1 test)` with `controlledChannelPresent=false`,
`controlledFixtureRecordPresent=false`, and `publicChannelCount=0`. The current androidTest APK,
focused guard/status checks, and serialized host aggregate include this fix. The general API
28/API 33/API 35 matrix uses the explicit producer bypass and is not producer-execution evidence.

The following same-digest Round 1 returned `ZERO ISSUES`, but Round 2 found that five API-35-only
screenshot evidence tests silently returned on a wrong API. The same pattern also existed in the
guarded P14 production capture. Both verdicts are invalid. All six now fail on a wrong API by
default and skip their capture bodies during a lower-API full regression only when
`nomeCrossApiScreenshotSkip=true` is explicitly supplied. On API 28, a focused class failed
without the argument and passed with it. The rebuilt test APK then passed the explicit API 28 and
API 33 lower-API 88-test runs and the normal API 35 88-test run recorded above.

Round 1 against review-manifest digest
`58c5176e45f945daa1ebf89053e797500c66a693766397a453474a4333157206`
then found that argument-driven producer/lifecycle androidTests returned silently when their
required arguments were absent, so the general 88-test matrix could falsely appear to execute
them. That verdict is invalid and does not count. All 13 parameterized controlled harnesses now
fail closed on absent or invalid required inputs and bypass only with explicit
`nomeControlledProducerSkip=true`. The exact API 28 negative/explicit-bypass proof, rebuilt
aggregate, and new three-device general regression are recorded above. Focused controlled fixture
runs remain the only evidence for actual producer/lifecycle results.

Round 1 against the next 70-file review-manifest digest
`dc1795ad033a9e7fa4701a8062debee3c755327abcbe4dfb550c0dd349726b3a`
found that the separate P14 guarded capture still returned silently when its exact opt-in token was
absent or invalid, contradicting the fail-closed summary. That verdict is invalid and does not
count. The P14 test now fails on API 35 without the exact token unless
`nomeControlledProducerSkip=true` explicitly identifies a general-regression matrix; the actual
capture still requires the fixed fixture and remains supported only by its focused guarded run.
The exact no-token API 35 run produced the expected failure in 0.106s; the explicit
general-regression bypass passed `OK (1 test)` in 0.046s and is not capture evidence.

The unsigned release APK/AAB sizes, hashes, badging, native ABIs, and unsigned state are exactly
the current values above. Current APK/AAB entry and focused DEX scans again found zero controlled
test class/tag, evidence path, unknown screenshot, or final-report leakage.

## Final freeze boundary

The final review input, two consecutive `ZERO ISSUES` rounds against the same self-verifying
71-entry `REVIEW_INPUT_SHA256SUMS`, verdict-only `FINAL_REVIEW_VERDICTS.md`, final manifest, and
local release-candidate checkpoint may be created only after every `OPEN` row above is closed with
reproducible evidence or a plan-authorized `NOT REACHABLE` / `TRUTHFULLY UNSUPPORTED` result.
External production signing, domain verification, store mutation, publication, and push remain
unauthorized.
