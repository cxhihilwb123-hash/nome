# API 35 renderer matrix

These PNGs were captured by the debug-only, non-exported deterministic evidence activity on the
`nome-api35-arm64` AVD. They prove native rendering only; they are not real-core success evidence.
