# P13 native comparisons

The canonical comparison is directional rather than pixel-identical: the approved reference uses
a framed concept device and the native capture uses the API 35 emulator at 1080×2400. Renderer-only
states remain labelled as fixtures.
