# Nome iOS 东京服务器完整模拟器验收

日期：2026-07-26（Asia/Shanghai）

## 结论

状态：**PASS_WITH_PRODUCTION_LIMITS**

本轮只修改和验证 iOS；Android、SimpleX Core、协议、数据库和消息状态机均未修改。
两台隔离模拟器完成真实 Core 互联，第三台干净模拟器完成最终新用户与冷启动回归。

## 真实双端链路

| 项目 | 结果 |
| --- | --- |
| 两端全新资料、官方 SMP/XFTP 预设 | PASS |
| 一次性邀请与联系人建立 | PASS |
| A 到 B、B 到 A 文字 | PASS |
| 图片发送、接收端显示并全屏打开 | PASS |
| 语音通话 | PASS，双方连接，界面显示“已连接（通过中继）” |
| 视频通话 | PASS，双方连接，界面显示“已连接（通过中继）” |

通话诊断使用 DEBUG 专用环境开关绕过模拟器 CallKit 干扰；生产构建和真机默认行为不受影响。
通话菜单与音频、视频动作增加稳定辅助功能标识，UI 测试目标已编译通过。

## 最终全新用户回归

- 欢迎页、创建本机身份、官方服务器说明、使用规则、版本说明和首页全部为简体中文。
- 设置名字后按顺序进入服务器说明和使用规则，不会返回资料设置页。
- 官方服务器构建默认关闭通知注册，不再尝试上游 `ntf4.simplex.im`。
- 最终 5 分钟模拟器日志中未出现 `ntf4.simplex.im` 或 `registerToken apiRegisterToken error`。
- 完成后直接进入首页；完全结束 App 再启动仍在首页，不回欢迎页或设置页。
- 首次 v6.5 版本说明补齐简体中文，没有本轮检查到的英文残留。
- 直接“加入群组”流程退出后会恢复通用扫码/粘贴路由，不污染后续“连接朋友”入口。

## 构建与静态检查

- Xcode 26.6，iOS 26.5 模拟器。
- 正常本地签名 App 构建：`** BUILD SUCCEEDED **`。
- App 与 UI 测试目标：`** TEST BUILD SUCCEEDED **`。
- `git diff --check`：PASS。
- `plutil -lint apps/ios/zh-Hans.lproj/Localizable.strings`：PASS。

## 生产限制

- 当前已部署并验证消息、文件和 TURN 中继，但尚未部署 Nome 自有 NTF/APNs 服务。
  因此 App 打开时可正常收发和通话；App 被系统挂起或完全关闭后的后台消息、来电提醒仍需
  独立 NTF 服务和正式 APNs 凭据后再验收。
- 模拟器可证明信令、WebRTC 和 TURN 中继建立成功，不能替代两台真机的麦克风、摄像头、
  锁屏来电和后台回收测试。
- App 内粘贴邀请链路已通过；网页短链接直接唤起 App 仍需要服务器 AASA、正式签名和真机
  Universal Links 闭环验证。

## 证据

原始截图保存在 Git 忽略的 `private/vultr-e2e-20260726/`，包括：

- 双向文字与图片接收/打开；
- `a-audio-connected-relay.png`；
- `a-video-connected-relay.png`；
- `c-network-no-upstream-ntf-final.png`；
- `c-whats-new-chinese.png`；
- `c-home-final-cold-restart.png`。

证据中未保存邀请链接、二维码、服务器口令或证书私钥。
