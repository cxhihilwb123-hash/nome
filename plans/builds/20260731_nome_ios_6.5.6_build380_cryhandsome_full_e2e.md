# Nome iOS 6.5.6 build 380 CryHandsome full E2E record

## Scope and final verdict

- Date and timezone: 2026-07-31, Asia/Shanghai.
- Authority repository: `/Users/forkman03/project/nome/nome-client`.
- Authority branch and HEAD at final audit: `codex/nome-v656-unified`,
  `73fbecde8c06c21f3b1dcec8fa4db2764860d466`.
- Physical endpoint: CryHandsome, iPhone 14 Pro, paired and connected.
- Independent peer: iOS 26.5 simulator B,
  `E4426929-A39D-45DD-96A6-2B6D41EC5B55`, profile `Nome B`.
- Tested app: Nome `6.5.6 (380)`, bundle
  `com.forkman03.nome.personal.v4.20260723`.
- Build 380 passes physical startup, existing-data preservation, direct text,
  image, video, voice-message, document, group, public channel, settings,
  production relay selection, and cold-start persistence checks.
- Audio calling is **not qualified**. Phone-to-simulator signaling left a
  missed-call record but produced no accept screen on the peer. The reverse
  simulator-to-phone action returned immediately to chat and produced no
  incoming/accept UI on CryHandsome. A second physical endpoint is required.
- Production APNs, notification/share extensions, and App Store/TestFlight
  packaging are outside this Personal Team build and were not tested.
- No uninstall, erase, reset, clean, stash, commit, push, tag, deployment,
  TestFlight upload, App Store action, or public release was performed.

## Build provenance

- Temporary physical-build worktree:
  `/private/tmp/nome-ios-cryhandsome-build376-20260730`.
- The worktree is detached at
  `3b060ddd3676f90b39e205be04c8ac8e3414cfee`, an ancestor of the authority
  HEAD. The intervening committed changes were Android-only.
- Before building, these final iOS files were copied into the temporary
  worktree and byte-compared with the authority checkout:
  - `apps/ios/Shared/Model/SimpleXAPI.swift`
  - `apps/ios/SimpleXChat/NomeLinkBranding.swift`
  - `apps/ios/PolicyTests/NomeActivationPolicyTests.swift`
- The Personal Team adaptations remained isolated to the temporary worktree:
  personal bundle identifier and team, main-app-only packaging, and unsupported
  production entitlements/extensions omitted.
- Build product:
  `/private/tmp/nome-cryhandsome-build380-derived-20260731/Build/Products/Debug-iphoneos/Nome.app`.
- Xcode device build result: `** BUILD SUCCEEDED **`.
- Main executable SHA-256:
  `7380286d0c8a16d5c1b1a4c4efbfd7df1a3276ed650e5a14b75918b973663e04`.
- Embedded `SimpleXChat` SHA-256:
  `53f5ca2f6db96d656eebc8f61d47262ade062dd7237ab4c27f6a8d2f36f7403b`.
- Required FFI symbols `_chat_migrate_init_key`, `_chat_recv_msg_wait`, and
  `_chat_send_cmd_retry` are present.
- The embedded current `a0e2b58d` / GHC 9.6.3 Core contains
  `_mblock_address_space`. Disassembly of `_initMBlocks` shows
  `0x100000000` (4 GiB), confirming the corrected iOS RTS reservation rather
  than the failing 256 GiB reservation from build 376.
- `codesign --verify --deep --strict` reports the expected host-side
  `CSSMERR_TP_NOT_TRUSTED` for the Personal Team trust chain. The phone accepted
  the signed app as an update and launched it; this is not an App Store signing
  qualification.

## Installation and startup

- Build 380 was installed over the existing v4 test bundle without uninstalling
  it. The separate older build 359 bundle remained untouched.
- Device readback after installation and again after testing returned Nome
  `6.5.6`, bundle version `380`.
- The first launch used the existing Nome app-lock flow and then opened the
  prior `CryHandsome QA` identity and chat history.
- Existing direct chats, the pre-existing channel `333`, `Relay QA 379`, and
  private notes remained present. No database migration error or startup crash
  occurred.

## Physical-device E2E matrix

| Area | Result | Live evidence |
|---|---|---|
| Launch and app lock | PASS | Build 380 launched, displayed the app-lock gate, unlocked, and reached Nome home. |
| Existing encrypted data | PASS | `CryHandsome QA` and all prior visible chats/history survived the overwrite install. |
| Direct text phone to B | PASS | `-B380-TEXT-20260731-0027` arrived on simulator B. The Chinese IME dropped the intended leading `PHONE` text during entry, so the displayed marker is recorded exactly. |
| Direct text B to phone | PASS | `SIM-B380-TEXT-20260731-0028` arrived on CryHandsome. |
| Image receive and open | PASS | B sent the repository's public 200x200 `tests/fixtures/logo.jpg`; CryHandsome rendered it inline and opened the full-screen image viewer. |
| Video receive and playback | PASS | B sent a synthetic two-second H.264 MP4; CryHandsome rendered its frame and playback advanced from `00:02` to `00:01`. |
| Voice message pipeline | PASS with input boundary | B recorded and previewed a real 20-21 second voice item, sent it, and CryHandsome playback advanced to `00:01`. CryHandsome also created and sent a voice item, but iPhone Mirroring displayed that the Mac cannot use the iPhone microphone, so physical acoustic capture on the phone is not claimed. |
| Document download and open | PASS | B sent `nome-b380-document.txt` (123 logical bytes). CryHandsome downloaded it and opened the system share/export sheet showing the same file name, type, and logical size. |
| Group create and invite | PASS | CryHandsome created `Build380 Group 0038`; B received and accepted the invitation. |
| Group text B to phone | PASS | `GROUP-B380-FROM-B-0039` arrived on CryHandsome. |
| Group text phone to B | PASS | `PHONE-GROUP-B380-0040` arrived on B with `CryHandsome QA` shown as owner. |
| Hosted channel link | PASS | CryHandsome shared its `333` owner link to B. B showed `Link signature verified` and opened the channel join flow. The token is intentionally omitted from this record. |
| Nome channel relay join | PASS | B joined through `Nome Relay`, recovered channel history, and remained a read-only subscriber. CryHandsome's subscriber count changed from one to two. |
| Channel broadcast | PASS | CryHandsome broadcast `CHANNEL-B380-OWNER-0043`; B received the exact marker. |
| Settings and privacy | PASS | Main settings, privacy controls, and the 6.5.6 About row rendered on CryHandsome. No user setting was changed. |
| Production server selection | PASS | `Nome Relay`, `smp.nome.im`, and `xftp.nome.im` were selected. Advanced network settings showed Web ports off and the standard unspecified-port TCP default of 5223. |
| Phone-to-B audio call | FAIL / not qualified | CryHandsome entered the encrypted outgoing call UI. Its console showed CallKit/audio activation followed by deactivation about four seconds later. B never displayed an incoming/accept screen; only a missed-call history item appeared. |
| B-to-phone audio call | FAIL / not qualified | The simulator's explicit `chat-audio-call` action returned immediately to chat. CryHandsome displayed no incoming or accept UI. |
| Universal-link handoff | NOT RUN | The Personal Team package omits the production Associated Domains entitlement. In-app hosted `/c` parsing and relay joining passed, but tapping a channel URL from Safari/Mail into Nome is not qualified by this package. |
| Production push/extensions | NOT RUN | The main-app-only Personal Team package omits the production notification service/share extensions and cannot qualify APNs. |

## Test-media identity

- Image source: `tests/fixtures/logo.jpg`, 200x200, SHA-256
  `fa0f2a48e4b3ff26706d4941afb224e129eb0374591175bc8fc1f998015977bd`.
- Video source: `/private/tmp/nome-e2e-video.mp4`, H.264 454x988,
  2.000 seconds, 27,536 source bytes, SHA-256
  `6b5a7b3a300d9c03b77e9000f22254fcfe86bb6ccce16b5e56282b0aa51fc46b`.
- Document source: `/private/tmp/nome-b380-document.txt`, 123 bytes,
  marker `DOC-B380-20260731-0034`, SHA-256
  `fd3ae5cd3e92966fd7a0e75e94d574c45d8760f336dec5d85d74c6f900c0a448`.
- Only repository-public or synthetic media was used. No personal photo or file
  was selected.

## Cold-start persistence and runtime evidence

- The attached device console recorded normal UIKit warnings and the expected
  audio-call session sequence. It did not record a spontaneous Nome crash.
- The console was ended deliberately with `Ctrl-C`; the resulting signal 9 is
  test teardown, not a product crash.
- `devicectl --terminate-existing` then launched build 380 successfully.
- Simulator B was independently terminated and relaunched with `simctl`.
- After both cold starts, CryHandsome still showed `Nome B`, channel `333` with
  `CHANNEL-B380-OWNER-0043`, and `Build380 Group 0038` with
  `PHONE-GROUP-B380-0040`.
- Simulator B independently recovered seven chats, including the same channel
  broadcast and group marker. This proves the new direct/group/channel state was
  committed and recoverable rather than only held in UI memory.

## Source checks and publication boundary

- `scripts/ios/test-nome-link-branding.sh`:
  `PASS: NomeLinkBranding (41 cases)`.
- `scripts/ios/test-nome-activation-policy.sh`:
  `PASS: NomeActivationPolicyReducers (42 cases)`.
- Temporary-worktree `git diff --check`: PASS.
- Final authority-worktree `git diff --check`: PASS.
- The authority checkout remains intentionally mixed and dirty. This record is
  added without staging or publishing any of those changes.
- Build 380 is qualified for the functions proven above, not for release. The
  remaining release gates are a connected call between two physical endpoints
  and production-signed APNs/extensions in the distribution package.
