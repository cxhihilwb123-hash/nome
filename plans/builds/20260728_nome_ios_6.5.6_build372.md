# Nome iOS 6.5.6 build 372

- Date: 2026-07-28 (Asia/Shanghai)
- Product: `Nome`
- Build product: `Nome.app`
- Standard archive label: `Nome-iOS-6.5.6-build372-20260728-Debug.app`
- Bundle ID: `com.forkman03.nome.personal.v4.20260723`
- Configuration: Debug, Personal Team physical-device build
- Installed device: CryHandsome, iPhone 14 Pro

## Changes

- Configure `Nome Relay` automatically once for each newly initialized user.
- Record successful initialization by the user's stable agent user ID.
- Preserve later user choices: disabling, deleting or editing the relay is not
  overwritten by subsequent app launches.
- Keep the existing Nome SMP/XFTP startup migration behavior unchanged.

## Verification

- Physical-device Xcode build: PASS (`** BUILD SUCCEEDED **`).
- Bundle metadata readback: `Nome 6.5.6 (372)`, expected bundle ID and relay
  configuration present.
- Upgrade installation over build 371: PASS; existing chats remained visible.
- Device-installed version readback: `6.5.6 (372)`.
- Per-user initialization marker read-only check: PASS; the temporary copied
  preferences file was deleted after checking.
- Disable `Use for new channels`, save and force-restart: PASS; it remained off.
- Re-enable, save and force-restart: PASS; it remained on.
- Final device state: `Nome Relay` enabled for new channels.

## Historical provenance

- Git repository/branch/full commit at build time: not recorded.
- `git status --short` at build time: not recorded; the recovered source record
  came from a dirty integration staging area.
- Controlled dirty patch: historical state not fully recorded.
- Artifact SHA-256: unavailable; no build 372 artifact was found in the Phase 0
  protected workspace or verified backup.
- This history is evidence, not a reproducible release record. Do not attach a
  later consolidated commit to this earlier binary.

## Distribution boundary

This is a directly installed Personal Team Debug app, not an exported IPA or a
public distribution build. No channel was created and no message was sent during
this settings-persistence test.
