# Nome iOS 6.5.6 build 374

- Date: 2026-07-28 (Asia/Shanghai)
- Product: `Nome`
- Build product: `Nome.app`
- Standard archive label: `Nome-iOS-6.5.6-build374-20260728-Debug.app`
- Bundle ID: `com.forkman03.nome.personal.v4.20260723`
- Configuration: Debug, Personal Team physical-device build
- Installed device: CryHandsome, iPhone 14 Pro

## Changes

- Restore the earlier compact chat status card: a single horizontal row at
  normal text sizes, with a Dynamic Type fallback for accessibility sizes.
- Shorten channel status chips to `中继` and the compact subscriber count.
- Replace channel security warnings with neutral relay-delivery copy in the
  header card, message details and channel creation guidance.
- Localize channel actions and supporting screens in Simplified Chinese,
  including channel link/share, profile editing, preferences, subscriber,
  relay, leave and delete actions.
- Replace the untranslated channel quick action `link` with `频道链接`.

## Verification

- `plutil -lint apps/ios/zh-Hans.lproj/Localizable.strings`: PASS.
- `git diff --check`: PASS.
- arm64 iOS Simulator build: PASS (`** BUILD SUCCEEDED **`).
- Physical-device Xcode build: PASS (`** BUILD SUCCEEDED **`).
- Upgrade installation over build 373: PASS; device data was preserved.
- Device-installed version readback: `6.5.6 (374)`.
- Physical UI verification through iPhone Mirroring: PASS.
  - Channel `369` shows the compact one-row header card.
  - The header uses neutral `消息通过频道中继稳定分发` copy and compact
    `中继` / `1 位` chips.
  - Channel details show `搜索`, `频道链接`, `静音`, `订阅者`, `和成员聊天`,
    `编辑频道资料` and `添加欢迎信息` in Chinese.

## Historical provenance

- Git repository/branch/full commit at build time: not recorded.
- `git status --short` at build time: not recorded; Phase 0 proved the recovered
  build 374 tree used a protected temporary source delta.
- Controlled dirty patch: yes, historical and recovered as evidence only.
- Artifact SHA-256: unavailable; no build 374 app/IPA artifact was found in the
  Phase 0 protected workspace or verified backup.
- The five temporary-only files were classified as Personal Team/device signing,
  container and Keychain adaptations. They were not applied to the consolidated
  branch because they would remove activation sources or gates.
- This history is evidence, not a reproducible release record. Do not attach a
  later consolidated commit to this earlier binary.

## Distribution boundary

This is a directly installed Personal Team Debug app, not an exported IPA or a
public distribution build. Verification only navigated existing channel data;
no channel was created, no message was sent and no data was deleted.
