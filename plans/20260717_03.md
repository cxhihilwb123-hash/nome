# Nome Android Phase 2 foundation → 下一线程权威交接

> 交接日期：2026-07-17（Asia/Shanghai）  
> 仓库：`/Users/forkman03/project/nome/simplex-chat`  
> 分支：`codex/nome-android-v656`  
> 官方基线：SimpleX Chat `v6.5.6` / `59fce95d3cd08897b4ef742447b785cf2e56c7ce`  
> 远端：`origin=https://github.com/simplex-chat/simplex-chat.git`，只读，不得 push  
> 当前结论：Phase 2 Android-only design foundation 已完成；production page shell、真实 state adapter、locale marker 和 GAP-16 高风险修正尚未实现

本文是最终 foundation 双轮审查之后新增的**交接元数据**，不属于
`REVIEW_INPUT_SHA256SUMS` 冻结的 223 项受审输入，也不回写已封存的 foundation
证据。本文与检查点记录由独立的 `plans/20260717_03_HANDOFF_SHA256SUMS` 绑定。

## 1. 新线程必须从这里开始

新线程在做任何修改前，按顺序完整阅读：

1. 本文 `plans/20260717_03.md`；
2. `apps/multiplatform/CODE.md`；
3. `plans/20260716_02.md`；
4. `plans/20260716_01.md`；
5. `plans/20260717_01.md`；
6. `plans/20260716_03.md`；
7. `plans/20260716_04.md`；
8. `plans/20260717_02.md`；
9. `apps/multiplatform/product/gaps.md`；
10. `apps/multiplatform/product/views/nome-android.md`；
11. `apps/multiplatform/spec/client/nome-android-ui.md`；
12. `plans/evidence/20260717_nome_android_phase2_foundation/README.md`；
13. `plans/evidence/20260717_nome_android_phase2_foundation/review-rounds.md`；
14. `plans/20260717_03_checkpoint.txt`。

随后先执行只读核验，不得把交接中的快照直接当成当前事实：

```sh
cd /Users/forkman03/project/nome/simplex-chat
git status --porcelain=v1 --untracked-files=all
git status --porcelain=v1 --untracked-files=all | wc -l
git rev-parse HEAD
git describe --tags --exact-match HEAD
git branch --show-current
git remote get-url origin
shasum -a 256 plans/evidence/20260717_nome_android_phase2_foundation/REVIEW_INPUT_SHA256SUMS
shasum -a 256 -c plans/evidence/20260717_nome_android_phase2_foundation/REVIEW_INPUT_SHA256SUMS
shasum -a 256 -c plans/evidence/20260717_nome_android_phase2_foundation/SCREENSHOT_SHA256SUMS
shasum -a 256 -c plans/20260717_03_HANDOFF_SHA256SUMS
```

`REVIEW_INPUT_SHA256SUMS` 与 `SCREENSHOT_SHA256SUMS` 的路径均以仓库根目录为基准。
Foundation evidence 总清单必须从其目录验证：

```sh
cd /Users/forkman03/project/nome/simplex-chat/plans/evidence/20260717_nome_android_phase2_foundation
shasum -a 256 -c SHA256SUMS
```

Phase 1 的历史 48 项证据只做完整性复核，不重新生成：

```sh
cd /Users/forkman03/project/nome/simplex-chat/plans/evidence/20260716_nome_android_phase1
shasum -a 256 -c SHA256SUMS
cd /Users/forkman03/project/nome/simplex-chat
```

## 2. 不可突破的边界

- 严格保留官方 `v6.5.6 / 59fce95d` 基线和当前 dirty tree；
- 不得 `reset`、`clean`、`stash`、`checkout` 覆盖或删除任何现有修改/证据；
- 不修改 Haskell/native core、`Core.kt`、数据库/archive format、协议、command/event
  contract 或消息状态机；
- 不开发 iOS，不扩散到 Desktop，不向 `commonMain` 添加 Nome UI；
- 不 pull、不改 remote、不 push；本交接没有创建 commit，后续也不得把“继续开发”
  自动解释为 commit 或远端 push 权限；
- 不重做 Phase 0/1、264 行状态矩阵、API 35 真实 core、既有 48/48 证据或双语明暗
  baseline；
- 不修改封存的 Phase 1/Foundation evidence；下一批使用新的 dated evidence root；
- debug fixture 永远不是生产状态真值，C 行能力不得由 mock、timer、静态 badge 或
  optimistic default 升格为 A/B。

## 3. 已完成且不得重做

### 3.1 冻结决策

- GAP-08～17 采用 `product/gaps.md` 已记录的推荐路线；
- GAP-18 已实现为 Nome `minSdk 28`，API 26–27 unsupported；
- 不做 exact-commit native rebuild，不修改 native/Haskell source；
- Dark Token v1 代表集固定为 P02、P07、P17、P21、P23；
- 暗色保持与 approved light baseline 相同的 IA、语义、尺寸、间距、状态与 48dp
  契约，只 remap surface、contrast、scrim/border 与 elevation；
- Android-only foundation source placement 已冻结并实现。

### 3.2 已实现 foundation

生产可用的 Android-only 基础位于：

```text
apps/multiplatform/common/src/androidMain/kotlin/chat/simplex/common/ui/nome/
  accessibility/
  components/
  theme/
  tokens/
```

它只包含 tokens、Material 2 theme adapter、surface/button/seven-state panel 和
accessibility primitives；没有 production Activity、route、presenter、第二份 model/API
或真实状态适配器。

Debug/test-only 内容位于：

```text
apps/multiplatform/android/src/debug/java/chat/simplex/app/nome/
apps/multiplatform/android/src/test/java/chat/simplex/app/nome/
apps/multiplatform/android/src/androidTest/java/chat/simplex/app/nome/
```

未来 production Activity/system host 的保留落点是：

```text
apps/multiplatform/android/src/main/java/chat/simplex/app/nome/
```

该目录在 foundation 批次仍为空。下一批如需新增 presenter/state adapter，必须先在新批次
计划中冻结精确 placement，并同步 `nome-android-ui.md`、`spec/impact.md`、
`product/concepts.md` 和 `CODE.md`；不得先写 source 再补理由。

### 3.3 已通过门禁

- Gradle unit/lint/debug assembly/androidTest assembly/release manifest：PASS；
- API 35 connected instrumentation：9/9，0 failures/errors/skips；
- lint：45 warnings，0 error/fatal；
- minSdk：app/common、debug/release merged manifest、APK badging 均为 28；
- API 28 clean launch：真实 unchanged v6.5.6 server/receiver 启动；
- API 35 same-package non-empty upgrade：真实 core 冷启动；chat database 和 preferences
  仅在 immediate post-install checkpoint 证明 byte-identical，agent DB 不作此声明；
- 96/96 API 35 原生截图：zh-CN/en × light/dark、七个 foundation 状态、100%/200%；
- 14 张 approved-light comparison/contact sheet；
- 48dp、TalkBack name/role/stateDescription/live region、200% 与 contrast：PASS；
- threat-model delta：已记录；
- 旧 Phase 1 `SHA256SUMS`：48/48；
- 同一冻结摘要连续两轮：`ROUND 1: ZERO ISSUES`、`ROUND 2: ZERO ISSUES`。

## 4. 权威证据与摘要

| 项目 | 当前冻结值 |
|---|---|
| Reviewed payload manifest | `plans/evidence/20260717_nome_android_phase2_foundation/REVIEW_INPUT_SHA256SUMS` |
| Reviewed payload manifest SHA-256 | `65fe4b88a5a8b4c8c22120ff61dec3edc4f684701aee61a09fb7e4294f8d875e` |
| Reviewed entries | 223 = 218 dirty payload files + 5 frozen Android design references |
| Final evidence manifest | `plans/evidence/20260717_nome_android_phase2_foundation/SHA256SUMS` |
| Final evidence manifest SHA-256 | `c98793eb9573cf0dca09e3599a95fc1d595937ab4afd8bbbb35ef563393c2a35` |
| Final evidence entries | 131/131 |
| Screenshot manifest | `plans/evidence/20260717_nome_android_phase2_foundation/SCREENSHOT_SHA256SUMS` |
| Screenshots | 96/96 |
| Phase 1 evidence | 48/48 |
| Final reviews | `/root/foundation_review_round1_darkfixed` and `/root/foundation_review_round2_frozen` |

Foundation 截图是带有“非实时 CORE / not live CORE”标记的 deterministic debug references；
它们证明 tokens/layout/copy/state/无障碍/截图管线，不证明生产 route、送达、relay、
authentication、permission、migration 或 core 状态。真实 core 证据单独位于 API 28/35
记录中。

## 5. 当前 dirty tree 与运行时

写入本交接包前的现场状态：

- `git status --porcelain=v1 --untracked-files=all`：221 条；
- tracked modified：11；
- untracked：210；
- 其中 218 个 payload 文件已由 frozen review manifest 覆盖；
- 另外 3 个是冻结清单自身、`review-rounds.md` 和最终 evidence `SHA256SUMS`；
- forbidden-path match：0；
- 没有 commit，没有 push；
- `emulator-5554` 已退出，现场 `adb devices -l` 为空。

新增本文、检查点和 handoff hash 后，dirty 条目应为 224（11 modified + 213
untracked）。这是预期的交接元数据增量，不得用 reset/clean/stash 消除。

## 6. 尚未完成

- production Android shell 与官方现有 navigation/route 的窄接入；
- 真实 `CoreBacked` / `Derived` / `ClientOperation` / `Unavailable` state adapters；
- one-time locale marker，以及“不覆盖已有用户语言”的迁移/恢复契约；
- P01–P24 任一 production page 的真实状态接入；
- GAP-16 archive/migration 高风险客户端修正；
- identities/chats/attachments/settings/locale persistence 的更深升级断言；
- release signing/domain/App Links/store 发布；
- commit/push。

## 7. 默认下一小批次

若用户在新线程没有重新指定范围，默认执行：

### Phase 2 Batch 2 — production shell + P07/P08 read-only home-route truth

本批只允许：

1. 先新建 dated batch plan 和 evidence root，冻结 production shell 与 state adapter 的
   精确 Android-only placement；
2. 在保留的 Android production host 路径做最小 shell/system-inset/back/theme 接入，
   不替换官方 navigation，不 fork `ChatModel` 或 API；
3. 只接同一 home route 上 P07 list/content 与 P08 startup/empty 的现有事实：
   `currentUser`、`localUserCreated`、`chatRunning`、`switchingUsersAndHosts`、
   `ChatModel.chats`、现有 filter/network facts 和既有 `apiGetChats`/事件；
4. 只实现能够从这些事实证明的 P07 populated/read-only list，以及 P08
   skeleton-loading、first-use、true-empty、filtered-no-result、initial-unknown/
   device-offline、core-stopped；矩阵中仍为 blocked 的状态只有在对应 adapter 与测试
   真正完成后才能解除，不得用 fixture 伪造；device-offline 只能来自平台 connectivity，
   不能推断 relay/server/global health；
5. 暂不加入 favorite mutation、profile switch、connection-card mutation、search、
   composer、message send、locale marker 或 GAP-16 修正，避免把首个 production 批次做大；
6. 使用 foundation 组件，不复制 tokens/theme，不创建第二状态源；
7. 对实际接入的全部状态完成 zh-CN/en × light/dark × 100%/200%、48dp、TalkBack、
   API 28/API 35 real-core、P07 与 P08 approved-light 原生截图对照、threat-model delta；
8. 冻结新 review manifest，并在同一摘要上连续完成两轮独立 ZERO ISSUES；
9. 不修改或重新生成本 foundation 和 Phase 1 的旧 evidence。

如果 source inspection 证明这一范围无法在 Android-controlled code 内保真，停止 source
扩张，先记录共享理由；只有确实需要 shared wrapper 时才允许另开范围，并强制
`desktopTest` 与 Desktop 视觉/行为回归。不得绕过 wrapper 直连 native core。
如果完整 P07/P08 home-route 状态集合超过单个小批次预算，必须在写 source 前把它拆成
“production shell contract”和“home truth adapter”两个连续批次；不得以“小批次”为由
接入一个会遗漏实际可达状态的半成品 adapter。

## 8. 下一线程完成定义

下一线程只有在以下条件全部满足后才能宣称 Batch 2 完成：

- 仍为官方 `v6.5.6 / 59fce95d`，当前旧 dirty tree 未被覆盖；
- scope 仍是同一条 P07/P08 home route 的 read-only production 小批次，没有跨页面扩张；
- 双语、明暗、每个适用状态、100%/200%、48dp、TalkBack 全部通过；
- API 28/API 35 均有新批次真实 core/device 证据；
- 原生截图与 approved P07、P08 light references 有可复核对照；
- 新批次 threat-model delta 已落盘；
- build/test/lint/release-isolation 通过；
- 新冻结摘要连续两轮 ZERO ISSUES；
- 没有 core/DB/protocol/message-state-machine/iOS 越界；
- 没有 commit/push，除非用户在新线程另行明确授权。

## 9. 可直接复制到新线程的启动指令

```text
在 /Users/forkman03/project/nome/simplex-chat 继续 Nome Android 原生开发。先完整阅读
plans/20260717_03.md，再按其第 1 节顺序完整阅读 CODE.md、既有计划、产品/spec 和证据；
第一步实时核对 HEAD/tag/branch/remote、git status、交接 hash、foundation 223/223、
截图 96/96 与 evidence 131/131。严格保留 v6.5.6 / 59fce95d 和当前 dirty tree，
不得 reset/clean/stash/checkout 覆盖，不改 Haskell/native core、Core.kt、数据库/协议/
消息状态机，不做 iOS，不 pull/push，不重做 Phase 0/1、264 状态矩阵、48/48、API 28/35
真实 core、双语明暗 foundation 或既有两轮 ZERO ISSUES。默认只执行交接第 7 节的
Phase 2 Batch 2：最小 production shell + P07/P08 read-only home-route truth；先冻结新批次
plan、Android-only placement 和新 evidence root，再小批实现；若完整状态集超出预算，
必须在写 source 前拆成 shell contract 与 home truth adapter 两个连续批次。所有实际状态
必须双语、明暗、100%/200%、48dp、TalkBack、真实 core、原生 P07/P08 对照、
threat-model delta，并在同一新 digest 上连续两轮 ZERO ISSUES。不要 commit 或推远端，
除非我在新线程明确授权。
```
